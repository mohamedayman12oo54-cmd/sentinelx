# Base URL of the real SentinelX Backend (per API_OVERVIEW.md: base path is /api/v1)
# Only used once MOCK_MODE is set to false in src/lib/apiClient.js
VITE_API_BASE_URL=https://api.sentinelx.example.com/api/v1
